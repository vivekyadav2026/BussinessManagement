<?php require 'vendor/autoload.php'; $app = require_once 'bootstrap/app.php'; $app->make(Illuminate\Contracts\Console\Kernel::class)->bootstrap(); $org = \App\Models\Organization::where('name', 'like', '%Evalyn%')->first(); if($org) { $org->business_type = 'restaurant'; $org->save(); echo 'Fixed ' . $org->name; } else { echo 'Not found'; }
